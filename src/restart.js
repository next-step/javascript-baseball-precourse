import randomnums from "./randomnums";

//재시작버튼 보이기
function showRestartButton() { 
    const restartbuttonshow = document.getElementById("restartButton");
    restartbuttonshow.style.display = "block";
};

//재시작문구
function showReplayMessage() {
    const replaymessage = document.getElementById("restartMessage");
    replaymessage.style.display="block";

};

